-- ============================================================
-- 清空所有假数据（恢复到干净状态，准备积累真实数据）
-- 在 Neon SQL Editor 或本地 SQLite 中执行
--
-- 执行顺序：
--   1. 先执行本脚本（删除假数据）
--   2. 再执行 reset_weights.sql（重置算法权重到初始状态）
--
-- 注意：
--   - 这会删除所有对局、行为记录、预测结果
--   - 玩家库会保留（如果你也想删除玩家，取消最后一行的注释）
--   - 身份库、行为库、版型库不会被删除
-- ============================================================

-- 1. 删除行为记录
DELETE FROM behavior_records;

-- 2. 删除对局玩家关联
DELETE FROM game_players;

-- 3. 删除预测打分记录
DELETE FROM prediction_scores;

-- 4. 删除预测结果
DELETE FROM predictions;

-- 5. 删除对局
DELETE FROM games;

-- 6. 清空算法权重（之后需要执行 reset_weights.sql 重新插入初始权重）
DELETE FROM algorithm_weights;

-- 7. 可选：删除所有玩家（取消下面这行的注释）
-- DELETE FROM players;

-- ============================================================
-- 执行完本脚本后，请务必执行 reset_weights.sql
-- 否则 algorithm_weights 表为空，预测算法无法正常工作
-- ============================================================
